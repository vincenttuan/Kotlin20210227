package com.ktest

fun main(args: Array<String>) {
    val chinese: Int? = 100
    val english: Int? = 90
    val math: Int? = 80
    // 若沒考則放入 null, 計算總分的時候以 0 分計算之
    // 請計算總分 = ?
    val sum = 0
    println(sum)
}